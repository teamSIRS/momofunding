import { Card, CardGroup } from "react-bootstrap";
import styled from "styled-components";

export const QnAWrapper = styled(CardGroup)`
  border: 0 0 1px 0;
  flex-direction: column;
`;

export const QnACard = styled(Card)`
  border: 0px;
`;
